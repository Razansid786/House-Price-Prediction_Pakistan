# Pakistani House price prediction

An implementation of a model(RandomForest) trained on pakistani house price dataset from kaggle and predicting house prices.
Project contains complete dvc+git versioning pipleline where the model and dataset are stored on a remote (local repo in this case) and tracked using github.
The model is deployed on a simple Flask app for testing.